version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
            name: docker-compose up
            command: |
                docker-compose -f ./docker-compose.yml up -d
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build

